# ATLAS A1 owns a single user's audio session, including while its screen is
# hidden or there is no graphical logind seat. Keep BlueZ endpoints registered
# without altering ALSA, capture devices, codecs, pairings or the audio server.
wireplumber.profiles = {
  main = {
    monitor.bluez.seat-monitoring = disabled
  }
}
