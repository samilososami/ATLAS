#!/usr/bin/env bash
set -euo pipefail

HELPER="/usr/local/lib/atlas/atlas-spotifyctl"

if [[ ! -x "$HELPER" ]]; then
  echo "atlas-spotify is not installed correctly: missing $HELPER" >&2
  exit 1
fi

exec "$HELPER" "$@"
