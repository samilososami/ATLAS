Generation mode: built-in image_gen
Input: /tmp/codex-clipboard-8f105502-e16e-40ea-b964-9a8fef55c27b.png
Output: atlas-front-t-pose-collar-lowered-v2.png
Note: requested pixel displacement is approximate with generative editing.

Use case: precise-object-edit. Input image is the EXACT edit target. Make a tiny localized adjustment ONLY: translate the blue V-shaped collar/chest stripe vertically DOWN by approximately 12 pixels in this 1254x1254 image. Just a few pixels, a subtle shift, not a redesign. Move the WHOLE V stripe down together, both side ends and center point. Preserve its exact shape, angle, thickness, width, two blue shades and clean edges; do NOT make it thicker, shallower or narrower. Fill the narrow strip exposed above it with the matching white shell shading and paint the same blue stripe at its slightly lower position. This creates a slightly larger white gap between the bottom of the black face screen and the blue collar. ALL OTHER PIXELS / FEATURES should stay unchanged: same FRONT view, same T-pose, exact hands arms feet, same blue wristbands (do not move wristbands), same body shape, antenna, ball, face screen, eyes, smile, palette, shading, dark navy background, canvas dimensions and framing. Do not change the face or move the robot. Do NOT create a back view or add a logo. The only requested change is lowering the existing collar a tiny amount.
